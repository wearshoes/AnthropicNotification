---
name: "Formatter: Add"
description: "Add a new notification platform formatter with full OpenSpec + TDD workflow"
argument-hint: "<platform_name>"
---

Add a new notification platform formatter.

Invoke the `formatter-add` skill to handle this command. Pass through the platform name argument.

Example: `/formatter:add dingtalk`
