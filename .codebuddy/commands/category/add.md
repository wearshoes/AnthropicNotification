---
name: "Category: Add"
description: "Add a new monitored URL category with full OpenSpec + TDD workflow"
argument-hint: "<name> <path>"
---

Add a new monitored URL category.

Invoke the `category-add` skill to handle this command. Pass through both arguments.

Example: `/category:add blog /blog/`
